<%= form_for @supplier, :url => company_suppliers_path(@company) do |f| %>
  <%= f.text_field 'name', :placeholder => 'Supplier Name' %>
  <%= f.submit 'Add Supplier', :class => 'button' %>
<% end %>
